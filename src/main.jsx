import React from 'react';
import ReactDOM from 'react-dom/client';
import LineMovementTracker from './LineMovementTracker.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LineMovementTracker />
  </React.StrictMode>,
);
